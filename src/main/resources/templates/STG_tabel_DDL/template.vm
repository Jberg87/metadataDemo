/*
DDL generated by Jesper's Pipeline
Owner: $owner
*/

CREATE TABLE IF NOT EXISTS $database.$schema.$table (
#set( $linePrefix = "  " )
#foreach( $loop_parameter in $loop_parameters )
$linePrefix$loop_parameter.column_name $loop_parameter.column_datatype #macro_getDdlDatatype( $loop_parameter.column_precision $loop_parameter.column_scale ) $loop_parameter.column_nullable
#set( $linePrefix = ", " )
#end
)

##VELOCIMACRO: Maak het juiste datatype
#macro( macro_getDdlDatatype $precision $scale )
#if ( $precision == 0 && $scale == 0 )
##anti-linefeed-comment
#elseif( $scale == 0 )
($precision)##anti-linefeed-comment
#else
($precision,$scale)##anti-linefeed-comment
#end
#end